1 + 1

val myVal = 2.toString() + 12.toString()

BigInt.probablePrime(100, scala.util.Random)

val one = math.sqrt(3)

val two = math.pow(one , 2)

"crazy" * 3

10 max 2

math.pow(2, 1024)

BigInt(2) pow 1024

import scala.math.BigInt._
import scala.util._

probablePrime(100, Random)

val myStr = "THis is string"

myStr(0)

myStr last

BigInt.probablePrime(100, Random).toString(36)


